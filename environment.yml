name: eng-pred
channels:
  - pytorch
  - nvidia
  - conda-forge
channel_priority: strict

dependencies:
  - python=3.11

  # core
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - seaborn
  - tqdm
  - pillow
  - pyyaml
  - pytest
  - requests

  # data / IO
  - pyarrow<21
  - polars
  - boto3
  - google-cloud-storage

  # HF / embeddings
  - sentence-transformers

  # serving / inference service
  - fastapi
  - pydantic
  - uvicorn

  # pytorch + cuda (conda)
  - pytorch
  - torchvision
  - torchaudio
  - pytorch-cuda=12.1

  # tooling
  - conda-lock

  # pip-only deps
  - pip
  - pip:
      - clearml
